# Transition actions

You can add actions to transitions, this indicates that _something_ happens when the transition is triggered.

These are also added to the transition name, along with the guards, after a slash. So, you may find yourself having a mess of information in the transition name. Not much to do about it.

It is also found in the left side menu:

![Transition actions](Resources/transition%20action.png)