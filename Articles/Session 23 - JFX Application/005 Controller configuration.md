# Controller configuration

Very briefly, dependency injection is a technique where a class is given its dependencies, rather than creating them itself. Usually this means it accepts them as constructor parameters.